# Shipclock

Shipclock is a small, dependency-free project timeline estimator. It turns four practical inputs into a likely ship date, a realistic range, a confidence level, and a planning note.

## Run locally

Open `index.html` in a browser, or serve the folder with any static file server:

```bash
python -m http.server
```

Then visit `http://localhost:8000`.
